using ScepWright.Server.Host;

CaKeyPassphrase.Interactive = true;   // real scepca session: allow the hidden CA-key passphrase prompt

return ServerCli.Run(args, System.Console.Out);

internal partial class Program { }
